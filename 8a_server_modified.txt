'''Module to create a server application for emotion detection application'''
from flask import Flask, request, render_template
from EmotionDetection.emotion_detection import emotion_detector

app = Flask(__name__)

@app.route('/')
def render_index():
    '''Landing page for flask server'''
    return render_template('index.html')

@app.route('/emotionDetector')
def emo_detect():
    '''Function to provide text input and invoke emotion detection module'''
    #text = request.form['textToAnalyze']
    text = request.args.get('textToAnalyze')
    resp = emotion_detector(text)
    #print(emotion_resp)
    if resp['dominant_emotion'] is None:
        output = "Invalid text! Please try again!."
    else:
        text1 = "For the given statement, \
        the system response is "
        text2 = ', '.join(f"'{k}': {v}" for k, v in resp.items())
        text3 = f". The dominant emotion is {resp['dominant_emotion']}."
        output = text1 + text2 + text3
    return output

if __name__ == "__main__":
    app.run(host="0.0.0.0", port=5000)
